import styled from "styled-components";

export const Container = styled.button`
  padding: 1rem 2rem;
  background: linear-gradient(180deg, #dcf60a 0%, #bff205 100%);
  line-height: 1.5rem;
  font-size: 1.125rem;
  font-weight: 600;
  letter-spacing: 0.015em;
  font-family: "Roboto";
`;
